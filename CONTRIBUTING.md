# Contributing Guide
1. Clone the repository: `git clone <repo-url>`
2. Copy `.env.example` to `.env` or set default values.
3. Build the project using Maven: `mvn clean package`
4. Deploy the generated `.war` file to Apache Tomcat 9 `webapps/`.